
# 🏛️ بنية نظام صيدليات المصريين المطور (Supabase Version)

تم استبدال Google Sheets بنظام **Supabase** لتوفير بنية تحتية احترافية وقوية.

## 🏗️ المكونات (Components):
1. **Frontend:** React + Tailwind CSS + Gemini AI integration.
2. **Database (Supabase DB):** قاعدة بيانات PostgreSQL لتخزين الطلبات (جدول `prescriptions`).
3. **Storage (Supabase Bucket):** لتخزين الصور والملفات الأصلية بجودة عالية (باكيت `attachments`).
4. **AI Engine:** Gemini 3 Flash لتحليل الصور وتحويلها لنصوص مهيكلة.

## 📊 هيكل جدول البيانات (Table Schema):
| الحقل | النوع | الوصف |
|-------|-------|-------|
| id | UUID | معرف فريد تلقائي |
| serial_number | Text | كود الطلب (MS-XXXXXX) |
| full_name | Text | اسم العميل |
| phone | Text | رقم الهاتف |
| address | Text | العنوان النصي |
| location_url | Text | رابط خرائط جوجل |
| branch | Text | الفرع المختار |
| insurance_company | Text | شركة التأمين |
| ai_extraction | Text | تحليل Gemini للروشتة |
| prescription_url | Text | رابط صورة الروشتة في Storage |
| card_url | Text | رابط صورة الكارنيه في Storage |
| id_front_url | Text | رابط وجه البطاقة الشخصية |
| id_back_url | Text | رابط ظهر البطاقة الشخصية |
| created_at | Timestamp | وقت الإرسال التلقائي |

---

## 🔐 تعليمات الإعداد:
1. أنشئ مشروعاً جديداً في [Supabase](https://supabase.com).
2. في SQL Editor، نفذ كود إنشاء الجدول والـ Buckets.
3. انسخ `SUPABASE_URL` و `SUPABASE_ANON_KEY` إلى ملف `constants.ts`.
4. تأكد من جعل الـ Bucket "attachments" عاماً (Public) ليتمكن الصيادلة من رؤية الصور.
